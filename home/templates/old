 <!-- Login -->
    <section class="text-gray-600 body-font">
        <div class="container px-5 py-20 mx-auto flex flex-wrap items-center">
            <div class="lg:w-2/4 md:w-1/2 md:pr-16 lg:pr-0 pr-0">
                <h1 class="title-font font-medium text-3xl text-gray-900">Welcome to Movie Recommender </h1>
                <p class="leading-relaxed mt-4">Explore now.</p><br/>
                {% if user.is_authenticated %}
                <p></p>
                {% else %}
                <a href="{% url 'login' %}"><button class="text-white bg-indigo-400 border-0 py-2 px-7 focus:outline-none hover:bg-indigo-500 rounded text-lg">Login</button></a>
                <p class="text-xs text-gray-500 mt-3">Don't have an account yet? <a href="{% url 'signup' %}">Sign Up Now.</a></p>
                {% endif %}
            </div>

            <div class="lg:w-2/4 rounded-lg p-8 flex flex-col md:ml-auto mt-10 md:mt-0">
                <div id="carouselExampleAutoplaying" class="carousel slide" data-bs-ride="carousel">
                    <div class="carousel-inner m-0">
                        <div class="carousel-item active">
                            <img src="{% static 'img/slider1.webp' %}" class="d-block w-100 h-80">
                        </div>
                        <div class="carousel-item">
                            <img src="{% static 'img/slider2.jpeg' %}" class="d-block w-100 h-80">
                        </div>
                        <div class="carousel-item">
                            <img src="{% static 'img/slider2.jpeg' %}" class="d-block w-100 h-80">
                        </div>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>

        <!-- {% if user.is_authenticated %}
         <p></p>
        {% else %}
        <div class="lg:w-2/6 md:w-1/2 bg-gray-100 rounded-lg p-8 flex flex-col md:ml-auto w-full mt-10 md:mt-0">
            <p class="leading-relaxed mt-4">To utilize the watchlist</p>
            <a href="{% url 'login' %}"><button class="text-white bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg">Login</button></a>
            <p class="text-xs text-gray-500 mt-3">Don't have an account yet? <a href="{% url 'signup' %}">Sign Up Now.</a></p>
        </div>
        {% endif %}
         -->
        </div>
    </section>